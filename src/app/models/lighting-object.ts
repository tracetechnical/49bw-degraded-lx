export class LightingObject {
  idx: number;
  name: string;
  topic: string;

  constructor(idx: number, name: string, topic: string) {
    this.idx = idx;
    this.name = name;
    this.topic = topic;
  }
}
